const TOPIC_META={ai:{label:'AI',emoji:'🤖'},'machine-learning':{label:'Machine Learning',emoji:'🧠'},'data-science':{label:'Data Science',emoji:'📊'},'generative-ai':{label:'Generative AI',emoji:'✨'},'ai-engineering':{label:'AI Engineering',emoji:'🛠️'},agents:{label:'Agents',emoji:'🧩'},python:{label:'Python',emoji:'🐍'},'computer-science':{label:'Computer Science',emoji:'💻'},'data-engineering':{label:'Data Engineering',emoji:'🗄️'},cloud:{label:'Cloud',emoji:'☁️'},ethics:{label:'Ethics',emoji:'⚖️'},governance:{label:'Governance',emoji:'🏛️'},'health-ai':{label:'Health AI',emoji:'🏥'},training:{label:'Training',emoji:'🎓'},networking:{label:'Networking',emoji:'🤝'},'creative-ai':{label:'Creative AI',emoji:'🎬'},'business-ai':{label:'Business AI',emoji:'💼'},'business-tech':{label:'Business Tech',emoji:'🚀'},innovation:{label:'Innovation',emoji:'🌟'},careers:{label:'Careers',emoji:'💼'},databases:{label:'Databases',emoji:'🍃'}};
async function loadData(){const [eventsRes,metaRes]=await Promise.all([fetch('./data/events.json'),fetch('./data/meta.json')]);const [events,meta]=await Promise.all([eventsRes.json(),metaRes.json()]);events.sort((a,b)=>new Date(a.start)-new Date(b.start));return {events,meta};}
function fmtDate(value,timezone='Australia/Melbourne'){return new Intl.DateTimeFormat('en-AU',{dateStyle:'medium',timeStyle:'short',timeZone:timezone}).format(new Date(value));}
function topicChip(topic){const meta=TOPIC_META[topic]||{label:topic,emoji:'🔹'};return `<span class="pill">${meta.emoji} ${meta.label}</span>`;}
function toGCalDate(value){return new Date(value).toISOString().replace(/[-:]/g,'').replace(/\.\d{3}Z$/,'Z');}
function eventCard(event){return `<article class="card"><div class="eyebrow">${event.emoji} ${event.city}, ${event.state}</div><h3>${event.title}</h3><p class="muted">${event.summary}</p><div class="meta"><span>${fmtDate(event.start,event.timezone)}</span><span>•</span><span>${event.format}</span><span>•</span><span>${event.organizer}</span></div><div class="chips">${event.topics.map(topicChip).join('')}</div><div class="actions"><a class="button" href="${event.canonical_url}">🔗 Source</a><a class="button" href="https://www.google.com/calendar/render?action=TEMPLATE&text=${encodeURIComponent(event.title)}&dates=${toGCalDate(event.start)}/${toGCalDate(event.end||event.start)}&details=${encodeURIComponent(event.summary+' '+event.canonical_url)}&location=${encodeURIComponent(`${event.venue_name}, ${event.city}`)}">🗓️ Add</a></div></article>`;}
function renderHome({events,meta}){const now=new Date();const upcomingEvents=events.filter(event=>event.status==='upcoming'&&new Date(event.start)>=now);document.querySelector('#stats').innerHTML=`<div class="metric"><strong>${meta.event_count}</strong><span class="muted">curated events</span></div><div class="metric"><strong>${meta.city_count}</strong><span class="muted">cities and hubs</span></div><div class="metric"><strong>${meta.featured_scope||'Australia-wide'}</strong><span class="muted">coverage</span></div>`;document.querySelector('#next-events').innerHTML=upcomingEvents.slice(0,6).map(eventCard).join('');document.querySelector('#city-grid').innerHTML=Object.entries(meta.city_counts).sort((a,b)=>b[1]-a[1]).map(([city,count])=>`<div class="card"><h3>${city}</h3><p class="muted">${count} launch events</p></div>`).join('');document.querySelector('#topic-grid').innerHTML=Object.entries(meta.topic_counts).sort((a,b)=>b[1]-a[1]).slice(0,12).map(([topic,count])=>{const info=TOPIC_META[topic]||{label:topic,emoji:'🔹'};return `<div class="card"><h3>${info.emoji} ${info.label}</h3><p class="muted">${count} events</p></div>`;}).join('');if(meta.next_event){document.querySelector('#next-callout').innerHTML=eventCard(meta.next_event);}}
function renderCalendar({events}){const groups=new Map();events.forEach(event=>{const key=event.start.slice(0,7);if(!groups.has(key))groups.set(key,[]);groups.get(key).push(event);});document.querySelector('#calendar-groups').innerHTML=Array.from(groups.entries()).map(([month,monthEvents])=>{const heading=new Intl.DateTimeFormat('en-AU',{month:'long',year:'numeric'}).format(new Date(`${month}-01T00:00:00`));return `<div class="month-group"><div class="section-header"><h2>${heading}</h2><span class="muted">${monthEvents.length} events</span></div><div class="grid cards">${monthEvents.map(eventCard).join('')}</div></div>`;}).join('');}
function renderCities({events,meta}){document.querySelector('#city-lists').innerHTML=Object.keys(meta.city_counts).sort((a,b)=>meta.city_counts[b]-meta.city_counts[a]).map(city=>{const cityEvents=events.filter(event=>event.city===city);return `<section class="panel"><div class="section-header"><h2>${city}</h2><span class="muted">${cityEvents.length} events</span></div><div class="grid cards">${cityEvents.map(eventCard).join('')}</div></section>`;}).join('');}
function renderTopics({events,meta}){document.querySelector('#topic-lists').innerHTML=Object.keys(meta.topic_counts).sort((a,b)=>meta.topic_counts[b]-meta.topic_counts[a]).map(topic=>{const info=TOPIC_META[topic]||{label:topic,emoji:'🔹'};const topicEvents=events.filter(event=>event.topics.includes(topic));return `<section class="panel"><div class="section-header"><h2>${info.emoji} ${info.label}</h2><span class="muted">${topicEvents.length} events</span></div><div class="grid cards">${topicEvents.map(eventCard).join('')}</div></section>`;}).join('');}
document.addEventListener('DOMContentLoaded',async()=>{const data=await loadData();const page=document.body.dataset.page;if(page==='home')renderHome(data);if(page==='calendar')renderCalendar(data);if(page==='cities')renderCities(data);if(page==='topics')renderTopics(data);});
